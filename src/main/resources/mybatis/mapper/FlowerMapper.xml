<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.nihility.mybatis.mapper.FlowerMapper">

    <select id="searchById" resultType="flower" parameterType="int">
        SELECT id, name_english, name_chinese, age, add_time, update_time
        FROM flower
        WHERE id = #{id}
    </select>

    <select id="searchAll" resultType="flower">
        SELECT id, name_english, name_chinese, age, add_time, update_time
        FROM flower
    </select>

    <!--<update id="updateByEntity" parameterType="garden">
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM garden WHERE id = #{id}
    </delete>-->

</mapper>